openapi: 3.0.1

info:
  title: openapi test
  version: 0.1.0
servers:
  - url: /api


paths:
  "/foo":
    post:
      operationId: post_foo
      responses:
        "200":
          description: test
  "/bar/{id}":
    parameters:
      - in: path
        name: id
        schema:
          type: string
    get:
      operationId: get_bar
      responses:
        "200":
          description: test
